import { FormAutofocusDirective } from './form-autofocus.directive';

describe('FormAutofocusDirective', () => {
  it('should create an instance', () => {
    const directive = new FormAutofocusDirective();
    expect(directive).toBeTruthy();
  });
});
